from tools import bash

def main():
  command = ''
  counter = 0
  for commandElement in sys.argv:
    if i!=0
      if i>1
        command += ' ' + commandElement
      else
        command += commandElement
    counter++
  bash(command)
